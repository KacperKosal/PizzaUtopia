@page "/register"
@using PizzaUtopia.Components.Elements.Input
@using PizzaUtopia.Components.Elements.Buttons

@inject NavigationManager NavigationManager

<link href="https://fonts.googleapis.com/css?family=Jua" rel="stylesheet">
<link rel="stylesheet" href="Profile.css">

<div class="container">
    <!-- Przycisk powrotu do poprzedniej strony -->
    <button class="back-button" @onclick="NavigateBack">
        <img src="Images/back-arrow.png" alt="Powrót" />
    </button>

    <!-- Logo i tytuł aplikacji -->
    <div class="logo-container">
        <img src="Images/logopizzy.png" alt="PizzaUtopia Logo" class="logo"> <!-- Logo aplikacji -->
        <h1>PizzaUtopia</h1> <!-- Tytuł aplikacji -->
    </div>
</div>

<!-- Formularz rejestracyjny -->
<div class="form-container">
    <h2>Rejestracja</h2> <!-- Nagłówek sekcji rejestracji -->
    <form id="registerForm">
        <!-- Formularz dla użytkownika -->
        <!-- Pole wprowadzania adresu e-mail -->
        <Input InputType="Enums.InputType.Email" placeholder="Wprowadź e-mail" />

        <!-- Pole wprowadzania hasła z możliwością pokazania/ukrycia -->
        <div class="password-container">
            <Input InputType="Enums.InputType.Password" placeholder="Wprowadź hasło" />
        </div>

        <!-- Pole potwierdzania hasła z możliwością pokazania/ukrycia -->
        <div class="password-container">
            <Input InputType="Enums.InputType.Password" placeholder="Potwierdź hasło" />
        </div>

        <!-- Przycisk rejestracji -->
        <Button OnClick="OnMenuClick">Zarejestruj się</Button>
    </form>
</div>

@code {
    /// <summary>
    /// Funkcja nawigacyjna, przekierowująca użytkownika do strony menu po kliknięciu przycisku "Zarejestruj się".
    /// </summary>
    private void OnMenuClick(object? sender, EventArgs args)
    {
        NavigationManager.NavigateTo("/menu");
    }

    /// <summary>
    /// Funkcja nawigacyjna, przekierowująca użytkownika do strony startowej po kliknięciu przycisku powrotu.
    /// </summary>
    private void NavigateBack()
    {
        NavigationManager.NavigateTo("/start");
    }
}
